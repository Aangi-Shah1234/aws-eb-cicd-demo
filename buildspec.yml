version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo "Build started on `date`"
      - echo "Building project..."
artifacts:
  files:
    - app.js
    - package.json
    - node_modules/**/*
